<odoo>

    <record id="widget_tree_view" model="ir.ui.view">
        <field name="name">marketplace.dashboard.widget.tree</field>
        <field name="model">marketplace.dashboard.widget</field>
        <field name="arch" type="xml">

            <tree>
                <field name="name"/>
                <field name="widget_type"/>
                <field name="model_name"/>
                <field name="metric"/>
            </tree>

        </field>
    </record>

</odoo>